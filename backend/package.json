{
  "name": "real-time-order-tracking",
  "version": "1.0.0",
  "description": "Real-time order tracking system using PostgreSQL LISTEN/NOTIFY and Socket.IO",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon src/server.js",
    "db:setup": "prisma generate && prisma db push && npm run db:trigger && npm run db:seed",
    "db:trigger": "node prisma/setupTrigger.js",
    "db:seed": "node prisma/seed.js"
  },
  "dependencies": {
    "@prisma/client": "^5.13.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "pg": "^8.11.5",
    "socket.io": "^4.7.5"
  },
  "devDependencies": {
    "nodemon": "^3.1.0",
    "prisma": "^5.13.0"
  }
}
